# Database
POSTGRES_DB=tasktracker
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_PORT=5432

# App ports (host machine)
BACKEND_PORT=8080
FRONTEND_PORT=80

# Spring Boot
SPRING_PROFILES_ACTIVE=docker

# Frontend (used at Docker build time; /api is proxied by nginx)
VITE_API_URL=/api
